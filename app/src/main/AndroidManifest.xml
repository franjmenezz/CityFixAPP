<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".Activity.Activity_ConexionDB"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CityFixAPP"
        tools:targetApi="31">
        <activity
            android:name=".Activity.Splash"
            android:exported="true"
            android:theme="@style/Theme.CityFixAPP">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity.Login"></activity>
        <activity android:name=".Activity.Registro"></activity>
        <activity android:name=".Activity.Configuracion"></activity>
        <activity android:name=".Activity.Usuario"></activity>
        <activity android:name=".Activity.Activity_Administrador" />

    </application>

</manifest>